@model IEnumerable<MyAppointer.Models.Jobs>

@{
    ViewBag.Title = "List of Jobs";
}


<br/>
@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@
<table font-size:1.35em;" >
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Id)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Title)
        </th>
        <th>
            @Html.DisplayName("Name")
        </th>

        <th>
            @Html.DisplayNameFor(model => model.Users.City)
        </th>

        <th>
            @Html.DisplayNameFor(model => model.JobTypes.Title)
        </th>
        <th>
            @Html.DisplayName("Phone Number")
        </th>


        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Id)
        </td>
        <td>

            <a href="@Url.Action("Details", new { id=item.Id})">@Html.DisplayFor(modelItem => item.Title)</a>

            @*@Str=modelItem => item.Title;
            @Html.ActionLink(Convert.ToString(modelItem => item.Title), "Details", new { id = item.Id })*@
            
            @*@Html.DisplayFor(modelItem => item.Title)*@
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Users.FullName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Users.City)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JobTypes.Title)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.JobPhone)
        </td>
        
        </tr>
}

</table>
